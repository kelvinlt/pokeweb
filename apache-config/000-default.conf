<Directory /var/www/html>
    Options FollowSymLinks
    AllowOverride All
    Require all granted

    # Enable access to all file types
    <FilesMatch "\.(css|js|jpg|png|gif|ico|svg|woff2)$">
        Require all granted
    </FilesMatch>

</Directory>
